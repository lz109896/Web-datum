```java

条件判断if
if (condition) {
    //
} else {
    //
}
只有单条语句也不建议省略花括号

多个if ... else 串联要注意判断顺序

要注意判断边界条件

浮点数不能用==判断

引用类型用==判断是否为同一对象，用 equals() 判断是否逻辑相等

用 equals() 注意避免 null

if 及 else if 后面“（）”中的表达式的值必须是 boolean 类型。
```
